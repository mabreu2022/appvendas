CREATE TABLE FCR_FAT (
    CODEMP         INTEGER NOT NULL,
    CODCLIENTE     INTEGER NOT NULL,
    NUMFAT         INTEGER NOT NULL,
    NUMPARC        NUMERIC(3,0) NOT NULL,
    SITUACAO       VARCHAR(1) NOT NULL,
    DATAEMISSAO    DATE NOT NULL,
    DATAVENCTO     DATE NOT NULL,
    DATAVENCTOORI  DATE NOT NULL,
    DATAREC        DATE,
    PROGINCLUSAO   VARCHAR(20),
    CODPORT        INTEGER NOT NULL,
    CODMOEDA       INTEGER,
    VLFATURA       NUMERIC(16,2) NOT NULL,
    VLFATORI       NUMERIC(16,2) NOT NULL,
    VLRECEBIDO     NUMERIC(16,2),
    VLDEDUCAO      NUMERIC(16,2),
    VLACRESCIMO    NUMERIC(16,2),
    VLLIQUIDO      NUMERIC(16,2),
    TPDOC          NUMERIC(2,0) NOT NULL,
    NUMDOC         INTEGER,
    NUMDOCAUX      VARCHAR(40),
    PERCJUROMORA   NUMERIC(3,2),
    PERCMULTA      NUMERIC(3,2),
    OBS            VARCHAR(1000),
    TPINC          NUMERIC(2,0) NOT NULL,
    USUINC         INTEGER NOT NULL,
    USUREC         INTEGER,
    USUCAN         INTEGER,
    DATAINC        TIMESTAMP NOT NULL,
    DATACAN        TIMESTAMP,
    DATAIMP        TIMESTAMP,
    USUARIO        INTEGER NOT NULL,
    DATAATU        DATE NOT NULL,
    HORAATU        VARCHAR(8) NOT NULL,
    SELECAO        CHAR(1),
    CODVEND        INTEGER,
    NUMNOTA        INTEGER,
    NUMMOV         INTEGER,
    DATAMOV        DATE,
    TPPARCELA      VARCHAR(1),
    VLTOTRECEBIDO  NUMERIC(14,2),
    VLENTRADA      NUMERIC(14,2),
    NUMPARCORI     INTEGER,
    DTINICONTRATO  DATE,
    DTFIMCONTRATO  DATE,
    CODIMOVEL      INTEGER,
	CONSTRAINT FCR_FAT_PK PRIMARY KEY (CODEMP, CODCLIENTE, NUMFAT, NUMPARC)
);